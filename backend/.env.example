# MySQL Connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=fishstock
DB_PASS=ChangeThisPassword123!
DB_NAME=fishstock

# Server
PORT=3000

# JWT (use random string in production)
JWT_SECRET=change-this-secret-to-random-256-bit-string

# Default admin (created on first run if not exists)
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=Fish@Stock2026
